# API base URL (no trailing slash).
# Recommended for `npm run dev`: leave empty — Vite proxies /api → http://localhost:3001
VITE_API_URL=

# Direct API (optional; server CORS must allow http://localhost:5173):
# VITE_API_URL=http://localhost:3001
